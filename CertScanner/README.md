Usage of certscanner:
  -asn string
        ASN Number to scan
  -in string
        A file contining subnets (default "subnets.txt")
  -m int
        Max Threads (default 25)
  -out string
        Output file (default "results.txt")
